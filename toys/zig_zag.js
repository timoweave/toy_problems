/**
 * @param {string} s
 * @param {number} numRows
 * @return {string}
 */
var convert = function(s, numRows) {
  const chars = s.split('');
  
};

if (typeof module === 'object') {
  module.exports = convert;
}